# GitHub Pages link / domain guide

## Option A — အလွယ်ဆုံး၊ အခမဲ့

လက်ရှိ GitHub username က `storypicture911-crypto` ဖြစ်ပြီး repository ကို `shinhtatehtar` လို့ rename လုပ်ပါ။

```text
https://storypicture911-crypto.github.io/shinhtatehtar/
```

Repository rename:

1. Repository → Settings → General
2. Repository name ကို `shinhtatehtar` ပြောင်းပါ
3. Rename ကိုနှိပ်ပါ
4. Settings → Pages မှာ `main` / `(root)` ကိုပြန်စစ်ပါ

## Option B — GitHub.io အတိုဆုံး

GitHub username ကို `shinhtatehtar` လို့ရနိုင်ပြီး repository name ကို `shinhtatehtar.github.io` လုပ်ရင်:

```text
https://shinhtatehtar.github.io/
```

Username availability မရှိနိုင်တာကြောင့် GitHub မှာအရင်စစ်ရပါမယ်။

## Option C — ကိုယ်ပိုင် domain

Domain ကို ဝယ်ထားမှသာ:

```text
https://shinhtatehtar.com
```

သို့မဟုတ်:

```text
https://www.shinhtatehtar.com
```

GitHub → Settings → Pages → Custom domain မှာ domain ထည့်ပြီး DNS provider ဘက်မှာ GitHub Pages records ပြင်ရပါမယ်။ Domain မပိုင်သေးခင် `CNAME` ဖိုင်မတင်ပါနဲ့။ မဟုတ်ရင် GitHub Pages deployment link ပျက်နိုင်ပါတယ်။
